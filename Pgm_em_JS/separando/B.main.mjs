// // 1- Principal Main
// // Recebe dinamicamente novos Objetos

export class New_Linguagem {
  constructor(
    linguagem,
  ) {
    this.linguagem = linguagem;
  }
}

// import { New_Linguagem } from "./B.compostos.mjs";

// export class MainLinguagem {
//   constructor(new_linguagem) {
//     this.linguagem = new New_Linguagem(new_linguagem);
//   }
// }
